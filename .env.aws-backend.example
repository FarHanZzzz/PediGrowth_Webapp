# AWS backend runtime environment variables
# Use these for App Runner, ECS task definitions, or EC2 service env

# Allow browser calls from your deployed frontend domain(s)
# Comma-separated when multiple origins are needed
CORS_ALLOW_ORIGINS=https://your-frontend.vercel.app,https://your-domain.com

# Uvicorn runtime
PORT=8000
